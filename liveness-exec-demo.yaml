apiVersion: v1
kind: Pod
metadata:
  name: liveness-exec-demo
spec:
  containers:
    - name: liveness-exec-demo
      image: ikubernetes/demoapp:v1.0
      livenessProbe:
        exec:
          command: ["/bin/sh", "-c", "curl -s http://127.0.0.1:80/livez | grep -q 'OK'"]
  restartPolicy: Never
